lib.gridelements {
	container < .defaultGridSetup
	container {
		# Render the children the default way
		columns.0 {
			renderObj = < tt_content
		}
	}
}

tt_content.gridelements_pi1.20.10.setup.container < lib.gridelements.container
